version: '3.8'

services:
  bot:
    build: .
    container_name: ruobr-bot
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env.prod
    volumes:
      - ./data-prod:/app/data
